import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'teslo-shop',
  webDir: 'dist/teslo-shop/browser'
};

export default config;
